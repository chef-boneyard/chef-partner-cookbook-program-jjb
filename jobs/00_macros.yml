- builder:
    name: check-markdown-files-are-there
    builders:
      - shell: |
          if [ ! -a CHANGELOG.md ]
            then
            echo "#"
            echo "#"
            echo "#"
            echo "# NO CHANGELOG.md "
            echo "#"
            echo "#"
            echo "#"
            exit 0
          fi

          if [ ! -a README.md ]
            then
            echo "#"
            echo "#"
            echo "#"
            echo "# NO README.md "
            echo "#"
            echo "#"
            echo "#"
            exit 0
          fi

          if [ ! -a TESTING.md ]
            then
            echo "#"
            cho "#"
            echo "#"
            echo "# NO TESTING.md "
            echo "#"
            echo "#"
            echo "#"
            exit 0
          fi
